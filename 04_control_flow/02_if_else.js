const balance = 200;
if (balance >500){
    let power = "fly";
    console.log(`you can ${power}`);
}else{
    let power = "not fly"
    console.log(`you can ${power}`);
}
// console.log(power); this will give erroe bcoz power is block scoped and cannot be accessible 

